<%- include('partials/header') %>

<main>
    <h1><%= title %></h1>

   <p><%= categoryDetails.category_name %></p>
    <ol>
           <% categoryProjects.forEach(categoryProject => { %>
                <li>
            
                    <a href="/project/<%= categoryProject.project_id %>"><%= categoryProject.title %></a>
                </li>
            <% }); %>
            
    </ol>
   
    <a href="/edit-category/<%= categoryDetails.category_id %>" class="other_pages_link">Edit Category</a>

</main>

<%- include('partials/footer') %>